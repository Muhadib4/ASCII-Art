import PresetGallery from '@/components/preset-gallery';
import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Presets — ASCII Creative Studio' };
export default function Page() { return <PresetGallery />; }
